# 资管业务原型展示站

## 文件结构

- `index.html`：原型导航首页
- `asset-workflow.html`：FOF 资产入池流程原型

## 发布

将本目录内的文件复制到 `ammo-ui` 仓库根目录，提交并推送后，可通过 GitHub Pages 作为长期原型展示站使用。

后续新增原型时，增加独立 HTML 文件，并在 `index.html` 的原型目录中增加对应卡片。
